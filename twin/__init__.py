# TERRA-Твин: цифровой двойник настоящего.
#
# Симуляция настоящего для реальной территории (первый регион — Залив
# Сан-Франциско): рельеф, здания, дороги, вода, погода — из открытых данных,
# с провенансом source/license/tier/retrieved как в атласе (ярус K).
# От слепка настоящего дальше строится путь назад во времени (слои эпох).
#
#   python3 -m twin.ingest --budget 1500      # резюмируемый сбор данных
#   python3 -m twin.doctor                    # сводка + физические проверки
#   python3 -m twin.build sf                  # scene.json.gz сцены
#   python3 -m twin.view sf                   # twin_sf.html — 3D-прогулка
