# Unreal binary assets must use a locking-capable LFS workflow when they are
# added to source control. Source, config, docs, and manifests remain normal Git.
*.uasset filter=lfs diff=lfs merge=lfs -text lockable
*.umap filter=lfs diff=lfs merge=lfs -text lockable
*.ubulk filter=lfs diff=lfs merge=lfs -text lockable
*.uexp filter=lfs diff=lfs merge=lfs -text lockable
*.uptnl filter=lfs diff=lfs merge=lfs -text lockable

