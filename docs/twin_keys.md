# Keys the twin can use

Generated by `python3 -m twin.keys --md` — edit `twin/keys.py`, not this
file, or the two will drift apart.

Everything the twin does today runs on **keyless** sources. Every key
below buys a specific new capability, named in its row. Nothing here is
required to build or run the twin.

**No payment card is needed for any of it except Google.** Of the 16 providers listed, 14
hand out a key against an email address and nothing else. Google Maps
Platform refuses to issue a key at all without a billing account, and
Anthropic bills per token — those two are marked, kept in their own
section, and are entirely optional: the twin's realism roadmap does not
route through either of them.

## Where keys live (and why never in git)

| | |
|---|---|
| **Ingest keys** (our machine) | environment variable, or `twin/.keys.json` — which is gitignored and written with mode 600 |
| **Browser keys** (Google, Anthropic) | typed into the page by whoever opens it, kept in that browser's `localStorage`, never serialised into the HTML or the scene |
| **In the repository** | never. `twin.doctor` greps the sources, the compiled scene and the built page for key-shaped strings and fails the build if it finds one |

A browser key is inherently visible to whoever opens the page — that is
what it means for a browser to send it. It cannot be hidden, only
**restricted**: bind it to your own domain (HTTP referrer), enable only
the APIs you actually call, and set a spending cap. That is why our page
asks the viewer for the key instead of shipping one.

```bash
python3 -m twin.keys                    # what is present, what is missing
python3 -m twin.keys --set 511 <token>  # store one (never printed back)
python3 -m twin.keys --md               # regenerate this document
```

## Съёмка и рельеф

### OpenTopography

**No payment card.** Email address and a form.

**Unlocks.** ЛИДАР 1 м (USGS 3DEP) вместо спутникового рельефа 30 м. Это прямое лекарство от известной болячки: Copernicus GLO-30 — поверхностная модель, она поднимает землю крышами. С 3DEP центр города встанет на настоящую землю.

- Sign up: https://portal.opentopography.org/ → Register → My Account → API key
- Cost: бесплатно; крупные выборки — по квоте
- Stored as: `TERRA_OPENTOPO_KEY`
- Lives: on our machine · code path: not wired yet

Steps:

1. Завести учётную запись на portal.opentopography.org
2. My Account → получить API key
3. python3 -m twin.keys --set opentopo <ключ>

### NASA Earthdata Login

**No payment card.** Email address and a form.

**Unlocks.** Единый вход к архивам NASA: тепловые снимки поверхности (ECOSTRESS, 70 м) — независимая ПОВЕРКА нашего тепловизора, а не картинка; ночные огни VIIRS; исторические серии.

- Sign up: https://urs.earthdata.nasa.gov/users/new
- Cost: бесплатно
- Stored as: `TERRA_EARTHDATA_USER` (+ `TERRA_EARTHDATA_PASS`)
- Lives: on our machine · code path: not wired yet

Steps:

1. Зарегистрироваться на urs.earthdata.nasa.gov
2. python3 -m twin.keys --set earthdata <логин>
3. python3 -m twin.keys --set earthdata_pass <пароль>

### USGS EarthExplorer (M2M API)

**No payment card.** Email address and a form.

**Unlocks.** ИСТОРИЧЕСКАЯ АЭРОФОТОСЪЁМКА с 1930-х и весь архив Landsat. Это подложка для прошлого: сегодня под ногами фотография 2022 года, а в 1938-м должна быть фотография 1938-го.

- Sign up: https://ers.cr.usgs.gov/register → затем запросить доступ к M2M API в профиле
- Cost: бесплатно; доступ к M2M подтверждают вручную (обычно за сутки)
- Stored as: `TERRA_USGS_USER` (+ `TERRA_USGS_TOKEN`)
- Lives: on our machine · code path: not wired yet

Steps:

1. Зарегистрироваться на ers.cr.usgs.gov
2. В профиле запросить Machine-to-Machine (M2M) access
3. Сгенерировать application token
4. python3 -m twin.keys --set usgs_m2m <логин>
5. python3 -m twin.keys --set usgs_token <токен>

### Copernicus Data Space Ecosystem

**No payment card.** Email address and a form.

**Unlocks.** Sentinel-2 (10 м, свежий снимок каждые 5 дней) — СЕЗОННОСТЬ и изменения: зелень летом и зимой, новые стройки, выгоревшие склоны. NAIP снимают раз в два года, этого мало для «сейчас».

- Sign up: https://dataspace.copernicus.eu/ → Register
- Cost: бесплатно
- Stored as: `TERRA_CDSE_USER` (+ `TERRA_CDSE_PASS`)
- Lives: on our machine · code path: not wired yet

Steps:

1. Зарегистрироваться на dataspace.copernicus.eu
2. python3 -m twin.keys --set copernicus <логин>
3. python3 -m twin.keys --set copernicus_pass <пароль>

### Mapillary

**No payment card.** Email address and a form.

**Unlocks.** Вторая уличная съёмка вдобавок к KartaView — гуще покрытие и готовая разметка объектов (знаки, столбы, деревья). Больше точек, где нашу геометрию можно поймать на вранье.

- Sign up: https://www.mapillary.com/dashboard/developers
- Cost: бесплатно
- Stored as: `TERRA_MAPILLARY_TOKEN`
- Lives: on our machine · code path: not wired yet

Steps:

1. Войти на mapillary.com, открыть Developers → Register application
2. Скопировать Client Token (MLY|…)
3. python3 -m twin.keys --set mapillary <токен>

### Cesium ion

**No payment card.** Email address and a form.

**Unlocks.** Мировой рельеф Cesium и хранилище готовых 3D-наборов. Отсюда же раздаются фотореалистичные плитки Google — но за них всё равно платит владелец ключа Google, так что обходным путём к ним это НЕ является.

- Sign up: https://ion.cesium.com/signup
- Cost: бесплатный уровень без карты; плитки Google через ion всё равно считаются по тарифу Google
- Stored as: `TERRA_CESIUM_TOKEN`
- Lives: on our machine · code path: not wired yet

Steps:

1. Зарегистрироваться на ion.cesium.com
2. Access Tokens → создать токен
3. python3 -m twin.keys --set cesium_ion <токен>

## Земля и вода

### 511.org (Bay Area)

**No payment card.** Email address and a form.

**Unlocks.** ЖИВОЙ ОБЩЕСТВЕННЫЙ ТРАНСПОРТ: GTFS-Realtime по каждому перевозчику Залива (Muni, BART, AC Transit…) — положение каждого вагона и автобуса раз в 15–30 с. Это то, чего в городе больше всего движется, и его нет ни у кого без ключа.

- Sign up: https://511.org/open-data/token
- Cost: бесплатно, форма имя+почта, токен приходит письмом сразу
- Stored as: `TERRA_511_TOKEN`
- Lives: on our machine · code path: not wired yet

Steps:

1. Заполнить форму на 511.org/open-data/token
2. Забрать токен из письма
3. python3 -m twin.keys --set fivetheleven <токен>

### aisstream.io

**No payment card.** Email address and a form.

**Unlocks.** СУДА в заливе живьём (AIS по вебсокету): паромы, контейнеровозы, буксиры. Залив без судов — не залив.

- Sign up: https://aisstream.io/ → вход через GitHub → API keys
- Cost: бесплатно
- Stored as: `TERRA_AISSTREAM_KEY`
- Lives: on our machine · code path: not wired yet

Steps:

1. Войти на aisstream.io через GitHub
2. Создать API key
3. python3 -m twin.keys --set aisstream <ключ>

## Воздух и орбита

### OpenSky Network

**No payment card.** Email address and a form.

**Unlocks.** История полётов, а не только текущий кадр: треки бортов за прошедшие сутки и месяцы. Наш живой слой (adsb.lol) знает только «сейчас» — с OpenSky можно проигрывать вчерашний день и сверять прогнозы движения с тем, что вышло.

- Sign up: https://opensky-network.org/ → Sign up, затем Account → API client (выдаёт client_id и client_secret, OAuth2)
- Cost: бесплатно для некоммерческого использования; лимит запросов выше у зарегистрированных
- Stored as: `TERRA_OPENSKY_ID` (+ `TERRA_OPENSKY_SECRET`)
- Lives: on our machine · code path: not wired yet

Steps:

1. Завести учётную запись на opensky-network.org
2. В личном кабинете создать API client — получить client_id и secret
3. python3 -m twin.keys --set opensky <client_id>
4. python3 -m twin.keys --set opensky_secret <client_secret>

### Space-Track.org

**No payment card.** Email address and a form.

**Unlocks.** Полный каталог объектов на орбите и АРХИВ элементов: небо над городом можно отмотать на любую дату. CelesTrak (без ключа) даёт только свежие элементы.

- Sign up: https://www.space-track.org/auth/createAccount
- Cost: бесплатно, учётная запись обязательна
- Stored as: `TERRA_SPACETRACK_USER` (+ `TERRA_SPACETRACK_PASS`)
- Lives: on our machine · code path: not wired yet

Steps:

1. Зарегистрироваться, подтвердить почту
2. python3 -m twin.keys --set spacetrack <логин>
3. python3 -m twin.keys --set spacetrack_pass <пароль>

## Атмосфера и стихии

### AirNow (EPA)

**No payment card.** Email address and a form.

**Unlocks.** Качество воздуха по часам — дымка и смог в кадре получают измеренную причину, а не художественную. В сезон пожаров это половина того, как выглядит небо над Заливом.

- Sign up: https://docs.airnowapi.org/account/request/
- Cost: бесплатно
- Stored as: `TERRA_AIRNOW_KEY`
- Lives: on our machine · code path: not wired yet

Steps:

1. Запросить ключ на docs.airnowapi.org/account/request/
2. python3 -m twin.keys --set airnow <ключ>

### NASA FIRMS

**No payment card.** Email address and a form.

**Unlocks.** Очаги пожаров со спутников (VIIRS/MODIS) почти в реальном времени — источник той самой дымки, и проверяемое событие для прогнозов.

- Sign up: https://firms.modaps.eosdis.nasa.gov/api/map_key/
- Cost: бесплатно, MAP_KEY выдаётся по почте сразу
- Stored as: `TERRA_FIRMS_KEY`
- Lives: on our machine · code path: not wired yet

Steps:

1. Запросить MAP_KEY на firms.modaps.eosdis.nasa.gov/api/map_key/
2. python3 -m twin.keys --set firms <ключ>

### OpenAQ

**No payment card.** Email address and a form.

**Unlocks.** Тот же воздух, но по всему миру и с историей — пригодится, когда твин выйдет за пределы Залива.

- Sign up: https://explore.openaq.org/register
- Cost: бесплатно
- Stored as: `TERRA_OPENAQ_KEY`
- Lives: on our machine · code path: not wired yet

Steps:

1. Зарегистрироваться, забрать ключ в личном кабинете
2. python3 -m twin.keys --set openaq <ключ>

## Прошлое

### IPUMS NHGIS

**No payment card.** Email address and a form.

**Unlocks.** Перепись по десятилетиям с 1790 года и исторические границы. Сейчас жители твина расселены по переписи 2020; с NHGIS население можно отматывать назад вместе с домами — город 1900 года получит своих жителей, а не сегодняшних.

- Sign up: https://uma.pop.umn.edu/nhgis/user/new
- Cost: бесплатно, учётная запись; ключ в разделе API
- Stored as: `TERRA_NHGIS_KEY`
- Lives: on our machine · code path: not wired yet

Steps:

1. Завести учётную запись IPUMS
2. Account → API keys → создать ключ
3. python3 -m twin.keys --set nhgis <ключ>

## Требует платёжную карту (можно не заводить)

### Google Maps Platform (Street View Static + Map Tiles)

**Requires a payment card.** Optional — skip it and nothing else breaks.

**Unlocks.** Фотореалистичный слой целиком: снимок улицы из той самой точки, вид сверху плитками и фотограмметрия города 3D-плитками. Единственное здесь, что стоит денег, — и единственное, что даёт настоящую фотореалистичность без генеративных выдумок.

- Sign up: https://console.cloud.google.com/ → проект → APIs & Services
- Cost: ПЛАТНО и ТРЕБУЕТ ПРИВЯЗАННОЙ КАРТЫ: без биллинг-аккаунта ключ не выдаётся вовсе, даже под бесплатный лимит. Тарифы: https://developers.google.com/maps/documentation/tile/usage-and-billing Документация плиток: https://developers.google.com/maps/documentation/tile Слой написан и ждёт ключа, но твин без него полон: карту можно не заводить.
- Stored as: `TERRA_GOOGLE_MAPS_KEY`
- Lives: in the viewers browser · code path: wired

Steps:

1. Создать проект в console.cloud.google.com и привязать биллинг (тут и потребуется карта — если её не хочется, пропустите весь этот пункт реестра)
2. Включить: Street View Static API, Map Tiles API
3. Credentials → Create credentials → API key
4. ОБЯЗАТЕЛЬНО ограничить ключ: Application restrictions → HTTP referrers → добавить свой адрес (bekzod25-terra-world.static.hf.space/*)
5. ОБЯЗАТЕЛЬНО ограничить по API: только те два, что включили
6. Поставить потолок расходов: Billing → Budgets & alerts
7. Ключ НЕ класть в репозиторий: открыть twin.html и вписать его в поле «Google» на самой странице — он останется в вашем браузере

### Anthropic API

**Requires a payment card.** Optional — skip it and nothing else breaks.

**Unlocks.** Свободный разговор с жителями прямо из браузера — уже работает в игровом слое play.html; тот же путь годится жителям твина.

- Sign up: https://console.anthropic.com/
- Cost: платно по счётчику
- Stored as: `ANTHROPIC_API_KEY`
- Lives: in the viewers browser · code path: wired

Steps:

1. Создать ключ в console.anthropic.com
2. Вписать его в поле на странице игры — он остаётся в браузере

