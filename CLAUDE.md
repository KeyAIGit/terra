# TERRA — контекст для Claude (Cowork / Claude Code)

Симулятор цивилизации с субагентными людьми + игровой слой. Владелец: Bekzad.
Курс: игра-перемещение по эпохам, 3D, путь к фотореализму через UE5.
Роли: Фейбл (Claude) — весь код, симуляция, данные, игра; пользователь — аккаунты, UE на ноутбуке.

## Что это

- `terra/` — пакет Python 3.11 (только numpy/scipy/Pillow; three.js вшивается в HTML).
- Мир детерминирован сидом: код + сид = тот же мир до последнего имени. Бинарники
  прогонов НЕ нужно хранить — они воспроизводятся (`terra new` ~1.5 ч кусками).
- Онлайн-витрина мира: https://bekzod25-terra-world.static.hf.space/
  (HF Space Bekzod25/terra-world; тяжёлые страницы на CDN Higgsfield).

## Модули

| файл | что |
|---|---|
| contracts.py | общие структуры: World, Polity, Person, Tech, Juncture; биомы |
| world.py | планета: тектоника, климат 14 000 лет, реки, руды, шоки |
| society.py | ёмкость земли, баланс почвы, войны, эпидемии, обрушение сложности |
| agents.py | люди: черты, потребности, субъективные убеждения, выбор действий |
| knowledge.py | 123 знания БЕЗ дерева технологий — только условия возможности |
| lang.py | языки: фонология, 29 звуковых законов, ветвление, имена всего |
| junctures.py | развилки: эвристика / Anthropic API / файловый оракул |
| sim.py | главный цикл, чекпоинты, ветвление (fork), реестр людей people.jsonl |
| play.py + _play_js.py | ИГРА: 3D-прогулка от первого лица по поселениям, диалоги с NPC |
| globe.py | 3D-глобус планеты (three.js, торговые пути, ночные огни) |
| report.py / compact.py / gallery.py / portrait.py | дашборд, лёгкий атлас, лица |
| cli.py | команды: new/continue/fork/compare/report/doctor/compact/oracle |
| tests/test_terra.py | 72 проверки: детерминизм, физика, ветвление — гонять после правок |
| atlas/ | НАЧАТО: сбор реальных исторических данных (Wikidata/Pleiades/Met доступны) |

## Команды

```bash
python3 -m tests.test_terra --slow    # обязательно после правок ядра
python3 -m terra new --seed 1 --to 500 --name terra-1
python3 step.py terra-1 500 470       # мир кусками: контейнер убивает фон >40 мин
python3 -m terra doctor terra-1       # самопроверка физики и целостности
python3 -m terra.play runs/terra-1    # собрать игру-прогулку (play.html)
python3 -m terra.globe runs/terra-1   # глобус
```

## Грабли, на которые уже наступали (НЕ повторять)

1. Истощение почвы без восстановления → мир откатывался к собирательству.
   Теперь soil_balance с пределом нагрузки. Не трогать без прогона тестов.
2. Тупики аффордансов: «bind» и «record» запирали лодку/полиспаст/ВСЮ письменность.
   Тест «весь каталог достижим» это ловит.
3. Прибавочный продукт ≠ незанятая ёмкость (у мальтузианцев всегда 0 → нет государств).
4. Любой обход set по клеткам — ТОЛЬКО отсортированный (иначе расходится с чекпоинта).
5. Никаких кэшей в атрибутах класса (ломают детерминизм между мирами).
6. Летопись: вес события масштабировать от населения; принуждение требует аппарата.
7. Обрушения держав — с периодом восстановления 450 лет.

## Игровой слой (play.html)

Три сцены из ДАННЫХ прогона (столица 500 н.э., бронза, неолит), NPC — реальные
люди из people.jsonl, диалоги из данных; поле для ключа Anthropic API →
свободный чат с NPC прямо из браузера (anthropic-dangerous-direct-browser-access,
claude-haiku-4-5). Проверка UI — Playwright+Chromium (--use-gl=swiftshader).

## Атлас исторических данных (следующий большой этап)

План в проекте claude.ai: «TERRA-Атлас». Суть: НЕ собирать «все знания», а
выжимки готовых датасетов (Wikidata, Pleiades, HYDE, p3k14c, Met/Smithsonian CC0)
в Parquet+DuckDB на HF datasets; каждая запись с источником, лицензией и ярусом
достоверности K/T/R (известно/типично/реконструкция); компилятор «библий сцены»
(эпоха × регион) → JSON для игры/UE. Проба уже в atlas/proba.jsonl.

## UE5 (когда пользователь поставит)

ГОТОВО: `python3 -m terra.export_ue runs/terra-1` → пакет `export_ue/`
(на сцену: terrain.obj+текстура, terrain_far, water, masks/ PNG16+веса,
buildings.json, people.json, meta.json, proxy_meshes/) + общие
import_terra.py (скрипт для встроенного Python UE 5.4+, источники в
terra/ue/) и README_UE.md. Сцены строятся из terra.play.build_scene_data —
ОБЩЕЙ точки с play.html; высотное поле — порт groundH из _play_js.py
(держать в синхроне). Экспорт сам гоняет самопроверки (OBJ, «здания на
рельефе» <30 см, PNG16, спавны людей) и падает ненулевым кодом при провале.
Мост к ноутбуку: Claude desktop app, папка проекта UE в доверенных.
UE в облачном контейнере не запускается — UE-скрипт писать консервативно,
каждый рискованный вызов с фолбэком.

## Правила

- Русский язык во всех интерфейсах и летописи; имена переменных латиницей.
- Симуляция — единственный источник истины; визуализация только читает.
- Всё, что показывается пользователю, проверять Playwright'ом со скриншотами.
- После правок ядра: tests + doctor. Прогоны гонять step.py кусками.
